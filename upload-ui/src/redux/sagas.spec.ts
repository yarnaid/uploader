// import { handleNewUploadFile } from "./sagas";

// describe("sagas", () => {
//   describe("handleNewUploadFile", () => {
//     it("should call uploadFile with file and metadata", () => {
//       // Arrange
//       const file = new File([""], "filename");
//       const metadata = { name: "filename", tags: [] };
//       const saga = handleNewUploadFile();
//       // Act
//       // Assert
//     });
//   });
// });
